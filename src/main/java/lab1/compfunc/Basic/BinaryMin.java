package lab1.compfunc.Basic;

public class BinaryMin {
    public int IntBinaryMin( int fRes, int gRes) {
        return Math.min(fRes, gRes);
    }
    public double DoubleBinaryMin( double fRes, double gRes) {
        return Math.min(fRes, gRes);
    }
}
